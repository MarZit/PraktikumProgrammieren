package guiLager;

import javafx.scene.control.Button;

public class ReservationButton extends Button {

	public ReservationButton(Item item) {
		setOnAction(e -> {
			//add Code to reserve
			item.setLent((byte)1);
		});
	}
}
